spring.datasource.url=jdbc:postgresql://localhost:5432/docharmonie_db
spring.datasource.username=umbertoemonds
spring.datasource.password=yoic

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

pring.jpa.hibernate.ddl-auto=update

# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.format_sql=true

enableLoggingRequestDetails=false
logging.level.org.springframework=ERROR

# The format used for the keystore. It could be set to JKS in case it is a JKS file
#server.ssl.key-store-type=CRT
# The path to the keystore containing the certificate
#server.ssl.key-store=classpath:keystore/docharmonie.crt
# The password used to generate the certificate
#server.ssl.key-store-password=eeEpx57LrBTGu
# The alias mapped to the certificate
#server.ssl.key-alias=DRHARMONY

#server.ssl.enabled=true